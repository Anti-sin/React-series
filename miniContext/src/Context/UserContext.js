import React from "react";

const UserContext= React.createContext()

export default UserContext;  //context is a provider which proveds with the vairable needed 
//user context is that variable basically it is a context created using the method called react.createcontext()


